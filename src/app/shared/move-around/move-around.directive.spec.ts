import { MoveAroundDirective } from './move-around.directive';

describe('MoveAroundDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveAroundDirective();
    expect(directive).toBeTruthy();
  });
});
